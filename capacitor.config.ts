import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.preparationmaster.app',
  appName: 'Preparation Master',
  webDir: 'dist'
};

export default config;
